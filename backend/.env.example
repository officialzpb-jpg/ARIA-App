# Environment Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database (PostgreSQL via Docker)
DATABASE_URL="postgresql://aria:aria123@localhost:5432/aria?schema=public"

# JWT Secret (generate a strong random string)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# CORS Origins (comma-separated)
CORS_ORIGIN="http://localhost:3000,http://localhost:8080,app://localhost"

# Redis (for caching and sessions)
REDIS_URL="redis://localhost:6379"

# External API Keys (for server-side operations)
# These are different from user-provided keys
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
SENDGRID_API_KEY=""

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# Logging
LOG_LEVEL=info
